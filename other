    } catch (err) {
        console.error('Failed to save card to JSON DB', err);
    }
  }; // <-- Closes the buildStatsPayload helper function

  res.json({ ok: true, item });
}); // <-- This is where your original }); belongs to close the app.post route!
