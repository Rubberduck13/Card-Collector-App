Please look at my entire project workspace and resolve all file connection errors. Specifically, sync server.js so that all require imports at the top match the variable names used inside the file. 

1. Ensure 'attributeParser' / 'attribute_parser' matches line 225 and correctly points to the file in the services folder.
2. Ensure 'walletEngine' / 'wallet_engine' matches line 230 and correctly links to the wallet_engine.js file.
3. Validate that all variables like 'safeName' use consistent capitalization throughout server.js.
4. Keep the working try/catch global route wrapper intact so the server doesn't crash.

Provide the fully updated code files so they work together seamlessly.
