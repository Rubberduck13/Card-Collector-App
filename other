  console.log("Raw text sent to parser:", report.fullText || report.text || req.body.name);
  const parsedData = attributeParser.parseAttributes(
    report.fullText || report.text || req.body.name || ""
  );
