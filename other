Yes, please go ahead and apply those two edits to `public/app.js` and wire up the `/api/grade` endpoint fully in `server.js` as you described. Go ahead and commit these fixes directly to the `phase1/scaffold` branch right now.
