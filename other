The card modal is displaying "Centering: [object Object]" for the detailed metrics breakdown. Please check public/app.js where the card modal data is rendered and ensure it extracts the specific properties (like vertical and horizontal strings) instead of rendering the whole object.
