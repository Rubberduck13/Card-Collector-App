Look at my `services/attribute_parser.js` file. Right now, the `years` array logic is entirely skipped and hardcoded to return an empty array `[]`. 

Please implement a robust year extraction system inside this function:
1. Use a regular expression to look for 4-digit years (like 1986, 2003, 2024, etc.) within the incoming `fullText`.
2. Ensure it only captures realistic sports card years (for example, years between 1940 and 2027).
3. If a 2-digit year format is found with a leading apostrophe or slash (like '96 or '03), safely convert it to its full 4-digit counterpart (1996 or 2003).
4. Dedup the results so the same year isn't returned twice, and return them cleanly inside the `years` array in the final object.

Provide the fully updated, self-contained `attribute_parser.js` code.
