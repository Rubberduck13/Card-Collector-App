Please fully implement the code for `services/wallet_engine.js` using the spec shown on screen. Write out the logic for pricingReport({ numericGrade, cardName, rawPsa10Price }), getModifierForGrade(grade) scaling from PSA 10 down to PSA 1, and getAllModifiers(). Include the requested guard clauses for missing/negative input values.
